exit
Artist.find(song_params(:artist_id)[:artist_id]).id
Artist.find(song_params(:artist_id)[:artist_id])

Artist.find(song_params(:artist_id)[:artist_id]
params
exit
song_params(:genre_id)[:genre_id].to_i
song_params(:genre_id)[:genre_id].to_s
song_params(:genre_id)[:genre_id]
song_params(:genre_id)
song_params(:name)[:name]
song_params(:name)
@song
